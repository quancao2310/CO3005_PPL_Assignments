number a <- 5
        string b <- "hello"
        bool c <- true
        dynamic d <- a
        dynamic e
        func main()
        begin
            e <- d
            writeNumber(e)
            return
        end
        